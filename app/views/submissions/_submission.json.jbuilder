json.extract! submission, :id, :project_id, :key, :challenge_id, :submission_text, :submission_time, :complete, :user_id, :admin_id, :created_at, :updated_at
json.url submission_url(submission, format: :json)